# Initial engineering run

This three-round live EDSL run is preserved unchanged. All 30 jobs were ingested,
with five rejected control attempts retained. The run exposed an ambiguity in
citation formatting and branch equality, then a missing current-round instruction:
several third-round actions and the final board repeated February even though the
registered schedule ended in March. It is not the main dated walkthrough.

The new run in `../dated-live` uses the same company case and explicit round/date
instructions. Re-running was an implementation correction, not selection for a
preferred company outcome. The main guide links to both records. No answer text
in either run was hand-edited.
